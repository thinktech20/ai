# Evals Workspace Notes

This `evals/` folder is a local workspace folder under `dbx` and is not itself a git repository.

## Branch context for related code

Primary code repo for current heatmap eval work:

- repo: `pw_sdg_ai_ser_repo`
- path: `dbx/pw_sdg_ai_ser_repo`
- active branch used for this work: `fsr_eval`

## Quick branch check commands

Run from `dbx`:

```bash
cd /home/u560060992/dbx/pw_sdg_ai_ser_repo
git branch --show-current
git status --short
```

## Note

If you return later and need to continue this stream of work, start by checking out branch `fsr_eval` in `pw_sdg_ai_ser_repo`.
